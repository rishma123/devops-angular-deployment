name: Deploy Portfolio

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Reset environment (very important!)
        run: |
          rm -rf node_modules
          rm -f package-lock.json
          rm -rf ~/.npm
          rm -rf ~/.cache

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: "npm"

      - name: Install latest Tailwind & PostCSS plugin (forced clean)
        run: |
          npm install -D tailwindcss@latest @tailwindcss/postcss@latest postcss@latest

      - name: Install app dependencies
        run: npm install

      - name: Show PostCSS config
        run: cat postcss.config.js

      - name: Build Angular project
        run: npm run build

      - name: Upload to S3
        run: aws s3 sync dist/portfolio-final/browser s3://rishma-portfolio-site --delete
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: eu-central-1

      - name: Invalidate CloudFront
        run: aws cloudfront create-invalidation --distribution-id EXQIR3MGHO7CU --paths "/*"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: eu-central-1
